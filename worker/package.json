{
  "name": "gist-sync",
  "private": true,
  "type": "module",
  "description": "Gist 동기화 서버: Claude 커넥터(MCP) + 앱 저장 API. Cloudflare Workers + D1. 의존성 없는 단일 파일",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.135.0"
  }
}
